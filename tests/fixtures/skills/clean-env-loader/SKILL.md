---
name: clean-env-loader
description: Loads local configuration from a .env file for a project.
allowed-tools: Bash(node setup.js)
---

# Env loader

Seeds and loads local configuration. It only reads `.env` on this machine and
never transmits it anywhere.
